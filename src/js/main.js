import { loadHeaderFooter } from "./utils.mjs";
import "./globalAuth.js"; // Import global auth functionality

// Load header and footer (footer setup is now handled automatically in loadHeaderFooter)
loadHeaderFooter();